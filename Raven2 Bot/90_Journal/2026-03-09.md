# 2026-03-09 – Raven 2 bot

## Сделано сегодня
- Обновил backend `raven2-bot` на `bot-debian` под новый формат JSON без `terramond_code`.
- Настроил IOMMU на rocketsrv (BIOS: SVM + IOMMU Enabled, ядро видит AMD-Vi, есть IOMMU-группы).
- Через `vfio-pci` отвязал вторую RTX 4090 (05:00.0/05:00.1) от хоста и отдал её в Win11-VM.
- Настроил GPU passthrough в Win11-VM (q35 + OVMF), Windows видит 4090, драйвер NVIDIA установлен.
- Установил через Epic Games клиент Raven 2, игра запускается и работает в Win11-VM.
- Проверил, что персонаж продолжает играть даже после закрытия RDP-сессии (VM живёт автономно).

## Текущее состояние
- Linux backend: принимает `POST /snapshots` с новым JSON (lot без `terramond_code`), автосоздаёт террамонды по имени.
- Windows VM: есть Python 3.14 + клиент, способный слать фейковые снапшоты; сейчас VM используется как «игровой» узел с Raven 2.
- Proxmox-хост: IOMMU и vfio настроены, вторая 4090 закреплена за Win11-VM, SSH и веб-интерфейс работают стабильно.

## Что делать дальше
- Настроить простой OCR-пайплайн в Win11-VM для экрана террамондов (скрин области + распознавание количества и цены).
- Собрать первый реальный JSON из OCR и отправить его в `/snapshots` вместо фейковых данных.
- Зафиксировать формат OCR-JSON и первые грабли в `03WindowsVMRaven2` и `02Bot/Bot - overview`.
- Подумать над минимальными health-check'ами и алертами (молчание клиента, рост 4xx/5xx, аномальные цены/объёмы).
