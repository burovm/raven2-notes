# 2026-03-08 – Инфраструктура + Obsidian + Git

## Что сделали сегодня

- Настроили SSH-доступ к VM `bot-debian` c Proxmox, проверили вход под root и под пользователем `bmv`.
- Включили вход по ключам для пользователя `bmv` на `bot-debian` (используем тот же ключ, что и для root).
- Добавили SSH-алиас `bot-debian-bmv` на локальной Windows-машине в `C:\Users\bmv\.ssh\config`.
- Создали структуру проекта для заметок в Obsidian-вольте `C:\Users\bmv\YandexDisk\Raven2 Bot`:
  - Папки: `00_Inbox`, `01_Infrastructure`, `02_Bot`, `03_Windows_VM_Raven2`, `90_Journal`.
- Завели базовые заметки:
  - `01_Infrastructure/Proxmox - rocketsrv`
  - `01_Infrastructure/VM - bot-debian`
  - `02_Bot/Bot - overview`
  - `02_Bot/DB - blogdb schema`
  - `03_Windows_VM_Raven2/Windows VM - план`
- Установили Git for Windows через `winget`, проверили, что `git --version` работает.
- Превратили Obsidian-vault в Git-репозиторий:
  - `git init` в `C:\Users\bmv\YandexDisk\Raven2 Bot`
  - настроили `git remote add origin` на GitHub-репозиторий `raven2-notes`
  - настроили `git config --global user.name / user.email`
  - сделали первый коммит и `git push -u origin main` (через авторизацию в браузере/Git Credential Manager).
- Установили и включили плагин **Obsidian Git**:
  - включили авто-commit и авто-push с интервалом (~10–15 минут),
  - оставили auto-pull либо выключенным, либо с редким интервалом, так как пока работаем только на одной машине.
- Обсудили дальнейший план:
  - добить инфраструктуру Linux-бота (перенос кода, Dockerfile, docker-compose, БД),
  - после этого переходить к настройке Windows-VM и Raven 2-клиента.
